# Recurse the directories defined in DIRS

ROOT = ../../..

include $(ROOT)/mak/config.mak

DIRS = \
	InstanceProvider \
	IndicationProvider \
	MethodProvider \
	PropertyProvider \
	QueryProvider \
	HelloWorldProvider

include $(ROOT)/mak/recurse.mak
