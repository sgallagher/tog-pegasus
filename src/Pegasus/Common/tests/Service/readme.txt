
This directory contains under it examples of Windows services:

    InstallService - installs a windows service
    RemoveService - removes a windows service
    BeeperService - a service that beeps every five seconds

You must use the Windows Services Applet to see the results.
