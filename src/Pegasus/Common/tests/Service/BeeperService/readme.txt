
This service may be installed into the Windows registry by typing 
"make install". And it may be removed by typing "make remove".

Note that in order for this service to work, you must put the directory
containing this file into the system environment (not just the user 
environment).

In order to do this, go to:

    "Settings -> Control Panel -> System -> Environment"

And add to the Path environment variable.
