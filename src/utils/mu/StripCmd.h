#ifndef _StripCmd_h
#define _StripCmd_h

#include "Config.h"
#include <vector>
#include <string>

// Usage: strip start-pattern end-pattern filename

int StripCmd(const vector<string>& args);

#endif /* _StripCmd_h */
