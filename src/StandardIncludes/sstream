#if defined(PEGASUS_OS_HPUX) || defined(PEGASUS_PLATFORM_WIN32_IX86_MSVC)
#include <strstream.h>

#ifndef HAVE_STRSTREAM
#define HAVE_STRSTREAM
#endif

#else
#include <sstream>

#ifndef HAVE_STREAM
#define HAVE_SSTREAM 1
#endif

#endif

