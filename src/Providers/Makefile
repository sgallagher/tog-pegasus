# Recurse the directories defined in DIRS
# This makefile simply recurses all of the toplevel repository directories
ROOT = ../..

include $(ROOT)/mak/config.mak

DIRS = generic windows unix

include $(ROOT)/mak/recurse.mak
