
To support privilege separation, define the following environment variable
before building.

    PEGASUS_ENABLE_PRIVILEGE_SEPARATION

cimserver is the executor program (see src/Executor)
cimservermain is the server program (see src/Server)
