ROOT=../..

include $(ROOT)/mak/config.mak

TEST_DIRS = \
    Common/tests \
    Repository/tests \
    Server/tests \
    Client/tests

DIRS = \
    Common \
    Repository \
    Protocol \
    Client \
    Provider \
    Server \
    getoopt \
    Compiler \
    $(TEST_DIRS)

# Compiler \
# Compiler/cmdline \

include $(ROOT)/mak/recurse.mak
