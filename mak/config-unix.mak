OS_TYPE = unix

DEPEND_INCLUDES = -I/usr/include/g++-3

RM = rm -f

RMDIRHIER = rm -rf

MKDIRHIER = mkdir -p 

EXE_OUT = -o

OBJ = .o

OBJ_OUT = -o

EXE =

LIB_PREFIX = lib

STATIC_LIB_SUFFIX = .a

SHARED_LIB_SUFFIX = .so

# Suffix to use on the link line
LIB_SUFFIX = .so

COPY = cp

TOUCH = touch

ECHO = echo
