
    (B) The executor should shutdown cimserver when it exits on fatal errors.

    (A) Chose special directory for local-authentication requests, allowing
        requests against these files to be policy-checked.

    (A) Implement policy checks.

    (A) Revisit ownership of Unix domain socket file.

    (A) Figure out how to deduce user used to run cimservermain.

    (A) Logging via syslog (implement log-level option).

    (A) How do we handle the syslog program names (cimservermain and 
        cimserver, or cimexecutor and cimserver)?

    (A) Implement setgid() and setuid() for HandleStartAgentProcess()
